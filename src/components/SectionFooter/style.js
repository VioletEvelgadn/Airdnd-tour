import styled from "styled-components";

export const FooterWrapper = styled.div `
  
    margin-top: 10px;
    display: flex;
    align-items: center;
    cursor: pointer;
    font-size: 17px;
    font-weight: 700;
    color: rgb(0, 0, 0);
    &:hover {
      text-decoration: underline;
    }
    .icon {
      margin-left: 5px;
    }
`
